to start the server:
# cd src/fritzing
# python manage.py runserver 8080
the port can be any, defaulting to 80 if not provided

then in a browser go to:
http://localhost:8080/parts_gen/choose/

8080 is, of course, the same port specified in the manage script call

then just follow the forms 