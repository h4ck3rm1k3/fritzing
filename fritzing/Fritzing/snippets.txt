// get the EditPart for a model element

	// via Viewer: works!
		IDiagramViewer viewer = someEditPart.getViewer();
		List editParts = viewer.findEditPartsForElement(
				EMFCoreUtil.getProxyID(elem), ShapeEditPart.class);
		editParts = viewer.findEditPartsForElement(
				p.eResource().getURIFragment(p), GraphicalEditPart.class);
		
	// via EditPart: does not work, don't know why
		SketchEditPart sketchEP = (SketchEditPart) viewer.getContents();
		System.out.println(sketchEP.getEditingDomain().getResourceSet().getResources());
		Wire wire = ((Sketch) ((View)sketchEP.getModel()).getElement()).getWires().get(0);
		System.out.println(sketchEP.findEditPart(sketchEP, wire));
		
	// via Diagram: does not seem to offer any related API
		Diagram diagram = FritzingDiagramEditorUtil.getActiveDiagram();
		System.out.println(diagram.getName());
		System.out.println(ViewUtil.getIdStr(diagram));